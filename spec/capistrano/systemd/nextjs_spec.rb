# frozen_string_literal: true

RSpec.describe Capistrano::Systemd::Nextjs do
  it "has a version number" do
    expect(Capistrano::Systemd::Nextjs::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
